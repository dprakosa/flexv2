OPENAI_API_KEY=

NEXT_PUBLIC_APP_NAME="Meeting Catch-Up Companion"
# Optional: override the model used for catch-up generation.
OPENAI_MODEL=
# Optional: override the Realtime transcription model (default gpt-4o-mini-transcribe).
OPENAI_TRANSCRIBE_MODEL=
# Required for the visual-context widget (serpapi.com, google_images engine).
# Without it the widget silently stays off.
SERPAPI_API_KEY=
